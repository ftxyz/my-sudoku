import React from 'react';
import SudokuGrid from './SudokuGrid'

const GamePage = () => {
  // 在这里编写你的数独游戏组件代码
  // 可以直接将第一版的React代码拷贝到这里

  return (
    <div>
      <h1>Sudoku Game</h1>
      <SudokuGrid />
    </div>
  );
};

export default GamePage;